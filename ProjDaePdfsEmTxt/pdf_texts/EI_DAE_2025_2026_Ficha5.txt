 1 / 11 
 DEI 
Departamento 
Engenharia 
Informática 
Desenvolvimento de Aplicações Empresariais – 2025-26-1S 
Engenharia Informática – 3.º ano – Ramo SI 
Worksheet 5 
 
Topics: bundling DTOs; exception handling; versioning, concurrency and locking. 
Part I. Bundling DTOs 
In worksheet 4 you have developed a page that shows the details of a student. This page needs to 
make two requests: one to retrieve the basic attributes of the student (username, name, etc.) and 
another to retrieve the list of subjects where the student is enrolled. Now, we are going to modify 
the code so that only one request is needed. 
1. Add a list of SubjectDTO to the StudentDTO class as an attribute, create the list for both 
constructors and add the corresponding getters and setters methods. 
Note: While we want to return a StudentDTO object with all subjects when we open the student 
details page, we still want to send a list of StudentDTO objects without the respective list of sub-
jects when we open the students’ index page with the table of students (why?).  
2. In StudentService, modify method getStudent() to bundle subjects in the response 
DTO: 
@GET 
@Path("{username}") 
public Response getStudent(@PathParam("username") String username) { 
  var student = studentBean.findWithSubjects(username); 
  var studentDTO = StudentDTO.from(student); 
  studentDTO.setSubjects(SubjectDTO.from(student.getSubjects())); 
  return Response.ok(studentDTO).build(); 
} 
3. Run the application and test the changed services using the HTTP Request file. 
4. In your Nuxt project, update the [username].vue page to only use the “student details” 
endpoint (student/{username}), removing any reference to the “subjects of a student” 
endpoint (student/{username}/subjects). 

Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 2 / 11 
5. Run the academics-client Nuxt project and test it.  
Part II. Exception Handling 
In any application we must identify what may go wrong and define how the application should 
behave if those situations occur. We will now address how to deal with three such situations re-
garding the create student feature of the Academic Management application. After these steps, 
you should apply this knowledge to all other situations that may go wrong in the application.  
Let us start by dealing with the situation where we want to persist an already existing entity and 
the one where we want to use a nonexistent entity. 
6. In the exceptions package, create the exceptions: MyEntityExistsException and 
MyEntityNotFoundException and add a constructor with one argument of type 
String.  
7. Change methods StudentBean::create() and CourseBean::find() so that: 
• A MyEntityExistsException is thrown if a student with the received username al-
ready exists in the database. 
• A MyEntityNotFoundException is thrown if there is no course in the database with 
the course code received as parameter. 
The IDE will prompt you to add them to the method signature. What this means?  
For example, if you have an exception called BazException, defined like this:  
public class BazException extends Exception { 
  public BazException(String message) { 
    super(message); 
  } 
} 
If you want the bar() method of an EJB service called FooBean to throw said BazException, 
you must add it to the signature as follows: 
public class FooBean { 
  public void bar() throws BazException { 
    var didItFailed = doSomeBusinessStuff(); 
    if (didItFailed) { 
      throw new BazException("baz happened!"); 
    } 
  } 
} 
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 3 / 11 
Pay attention to the method signature. If a method doesn’t contain a try-catch block for the code 
that may produce an exception i t must “pass it up ” to the caller with code  like: throws 
BazException. For multiple exceptions, just separate with a comma: 
public class FooBean { 
  public void bar() throws BazException, ReallyBadStuffException { 
    var didItFail = doSomeBusinessStuff(); 
    if (didItFail) { 
      throw new BazException("baz happened!");  
    } 
  } 
} 
Note: some exceptions , like RuntimeException, are “unchecked exceptions” and don’t re-
quire to be caught or thrown.  
8. Add appropriate messages for each exception in all methods of all EJBs. Your resulting 
code should look like this:  
@Stateless 
public class StudentBean { 
  // ... code ... 
 
  public void create(String username, String password, String name, String email, 
    long courseCode) throws MyEntityExistsException, MyEntityNotFoundException { 
    if (entityManager.find(Student.class, username) != null) { 
      throw new MyEntityExistsException( 
        "Student with username '" + username + "' already exists"); 
    } 
 
    var course = courseBean.find(courseCode); 
    var student = new Student(username, password, name, email, course); 
    entityManager.persist(student); 
    course.addStudent(student); 
  } 
} 
 
@Stateless 
public class CourseBean { 
  // ... code ... 
 
  public Course find(long code) throws MyEntityNotFoundException { 
    var course = entityManager.find(Course.class, code); 
    if (course == null) { 
      throw new MyEntityNotFoundException("Course " + code + " not found"); 
    } 
    return course; 
  } 
} 
 
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 4 / 11 
As previously noted, if an exception may be thrown inside a method, it must be either caught 
by a try-catch block or propagated up  using throws in its signature (unless it’s an unchecked 
exception). For EJBs, we will use the try -catch approach. For services, we will use the throws 
in signature approach.  
9. Update ConfigBean::populateDB to catch the exceptions thrown by  
StudentBean::create() and display a n appropriate  message to System.err. For ex-
ample: 
try { 
  studentBean.create("joão", "123", "João", "joao@mail.pt", 1); 
} catch (Exception e) { 
  System.err.println("Some exception happened while creating student João"); 
} 
10. Update StudentService::createNewStudent() method signature to throw all excep-
tions: 
@POST 
@Path("/") 
public Response create(StudentDTO studentDTO) throws Exception { 
11. Try creating a student with a duplicate username and a student with a non -existing 
course code.  
12. Add the following attribute to the ConfigBean EJB to use a dedicated logger with 
java.util.logging.Logger:  
private static final Logger logger = Logger.getLogger("ejbs.ConfigBean"); 
13. Update all the try -catch blocks in populateDB() with calls to StudentBean::create() 
so that the logger receives the exception message as a “severe” event. Test it again with 
both a student with a duplicate username and a student with a non-existing course code. 
Your resulting code should look something like this:  
@PostConstruct 
public void populateDB() { 
  System.out.println("Hello Java EE!"); 
 
  courseBean.create(9119L, "EI"); 
 
  subjectBean.create( 
    1L, 
    "Desenvolvimento de Aplicações Empresariais", 
    3, 
    "2023/24", 
    9119L 
  ); 
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 5 / 11 
 
  try { 
    studentBean.create("foo", "bar", "foo", "foo@bar.com", 9119L); 
    studentBean.create("foo2", "bar", "foo2", "foo2@bar.com", 1000L); 
  } catch (Exception e) { 
    logger.severe(e.getMessage()); 
  } 
  studentBean.enroll("foo", 1L); 
} 
You should find this line in your logs file: 
. . . logs . . . 
. . . logs . . . 
academics-webserver-1  | 02:13:29,887 SEVERE [ejbs.ConfigBean] (ServerService 
Thread Pool -- 102) Course with code '1000' not found 
. . . logs . . . 
. . . logs . . . 
Next, we will handle exceptions thrown inside services methods.  
14. Create the MyEntityExistsExceptionMapper class (in exceptions.mappers package) 
with the following code: 
@Provider 
public class MyEntityExistsExceptionMapper  
  implements ExceptionMapper<MyEntityExistsException> { 
  private static final Logger logger = 
              Logger.getLogger(MyEntityExistsException.class.getCanonicalName()); 
 
  @Override 
  public Response toResponse(MyEntityExistsException e) { 
    String errorMsg = e.getMessage(); 
    logger.warning("ERROR: " + errorMsg); 
    return Response.status(Response.Status.CONFLICT) 
      .entity(errorMsg) 
      .build(); 
    } 
} 
15. Create a similar MyEntityNotFoundExceptionMapper that generates a response with 
the NOT FOUND status code. 
16. Finally, create mappers for exceptions IllegalArgumentException and Throwable 
(generic or “catch -all” exception). The first one should return a BAD_REQUEST response 
code. The second one should return an INTERNAL_SERVER_ERROR response code.  
Note: The @Provider annotation in the above *Mapper classes tells the JAX -RS runtime to 
discover these classes when one of the mapped exceptions is thrown (and not explicitly caught) 
and run the toResponse() method.  
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 6 / 11 
17. Modify all the service methods of the StudentService and CourseService classes ac-
cording to this exception handling approach. 
18. Run the application and test the services through the HTTP request file (alter the re-
quests so that exceptions are thrown). 
Let’s now deal with the ConstraintViolationException exception, which should be caught 
when we have entity attributes validation annotations that can be violated when we persist or 
update entities: 
19. Create the MyConstraintViolationException exception: 
package pt.ipleiria.estg.dei.ei.dae.academics.exceptions; 
 
import java.util.stream.Collectors; 
import jakarta.validation.ConstraintViolation; 
import jakarta.validation.ConstraintViolationException; 
 
public class MyConstraintViolationException extends Exception { 
  public MyConstraintViolationException(ConstraintViolationException e) { 
    super(getConstraintViolationMessages(e)); 
  } 
 
  private static String  
                 getConstraintViolationMessages(ConstraintViolationException e) { 
    return e.getConstraintViolations() 
            .stream() 
            .map(ConstraintViolation::getMessage) 
            .collect(Collectors.joining("; ")); 
  } 
} 
20. Create the corresponding mapper, returning a BAD_REQUEST response code. 
21. In the StudentBean::create() method, add a try -catch block around the code neces-
sary to persist the student entity, catch the ConstraintViolationException exception 
and throw a MyConstraintViolationException. The result must be something like the 
following code: 
public void create(String username,  
                   String password, String name, String email, Long courseCode) 
  throws MyEntityNotFoundException, MyEntityExistsException,  
  MyConstraintViolationException { 
 
  // ... code before ... 
 
  try { 
    var student = new Student(username, password, name, email, course); 
    course.addStudent(student); 
    entityManager.persist(student); 
    entityManager.flush(); // when using Hibernate, to force it to throw a 
ConstraintViolationException, as in the JPA specification 
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 7 / 11 
  } catch (ConstraintViolationException e) { 
    throw new MyConstraintViolationException(e); 
  } 
} 
22. Run the application and test it. Alter code so that exceptions are thrown. 
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 8 / 11 
Part III. Controlling concurrent access to entity data with locking 
Entity data is concurrently accessed if the data in a data  source is accessed at the same time by 
multiple applications/execution contexts. When data is updated in, for instance, database tables 
in a transaction, the persistence provider assumes the database management system will hold 
locks to maintain data integrity. Most persistence providers will delay database writes until the 
end of the transaction, except when the application explicitly calls for a flush.  
By default, persistence providers use optimistic locking, where, before committing changes to 
the data, the persistence provider checks that no other transaction has modified or deleted the 
data since the data was read. This is accomplished by a version column in the database table, 
with a corresponding version attribute in the entity class. When a row is modified, the version 
value is incremented. The original transaction checks the version attribute, and if the data has 
been modified by another transaction, a javax.persistence.OptimisticLockException will 
be thrown, and the original transaction will be rolled back. When the application specifies opti-
mistic lock modes, the persistence provider verifies that a particular entity has not changed since 
it was read from the database even if the entity data was not modified.  
We can ask how the provider can know whether somebody made changes in the intervening 
time since the committing transaction read the entity. The answer is that the provider maintains 
a versioning system for the entity. For it to do this, the entity must have a dedicated persistent 
field or property declared in it to store the version number of the entity that was obtained in the 
transaction. The version number must also be stored in the database. When going back to the 
database to update the entity, the provider can check the version of the entity in the database to 
see if it matches the version that it obtained previously. If the version in the database is the 
same, the change can be applied, and everything goes on without any problems. If the version 
was greater, somebody else changed the entity since it was obtained in the transaction, and an 
exception should be thrown. The version field will get updated both in the entity and in the da-
tabase whenever an update to the entity is sent to the database. Version fields are not required, 
but their use is recommended for portability reasons. 
NOTES 
• For a better debugging mode, don’t forget to always check your logs with the make moni-
tor command. When things don’t work, please try to understand the exception that 
probably occurred in the server, it should be printed into the logs. 
• Be patient: try to understand the logs.  
23. Add the following field to the User, Course and Subject entities: 
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 9 / 11 
@Version 
private int version; 
Version fields should not be modified by the application after the entity has been created. 
You do not need to change the constructors nor have getter and setter methods for these 
version attributes. 
If you prefer, you can create a mapped superclass Versionable, by doing the following:  
24. Create a new Java Entity class named Versionable: 
@MappedSuperclass 
public class Versionable { 
  @Version 
  private int version; 
} 
25. Make the User, Course and Subject inherit from Versionable.  
If you are wondering what @MappedSuperclass does, you should read additional documenta-
tion to understand the difference between the User entity superclass and the Versionable 
mapped superclass: 
• Hibernate 
Our current ORM implementation. 
• Eclipse Foundation (EclipseLink) 
Not used by us (another ORM implementation). Still helpful because, by quoting them, 
you understand that this is our goal for the exercise: 
“A mapped superclass is useful for defining a common persistence superclass 
that defines common behavior across a set of classes, such as an id or version 
attribute.” 
• Baeldung 
Really helpful if you don’t understand the difference from the User superclass. Explains 
all the inheritance strategies in a nutshell. 
CHECKPOINT 
Deploy your application, check that the logs are good and query the database to see that the col-
umn version appears in the tables of the mentioned classes. 
By default, JPA assumes what is defined in the ANSI/ISO SQL specification and known in trans-
action isolation parlance as Read Committed isolation. This standard isolation level simply 
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 10 / 11 
guarantees that any changes made inside a transaction will not be visible to other transactions 
until the changing transaction has been committed.  
An application may increase the level of locking for an entity by specifying the use of lock modes. 
Lock modes may be specified to increase the level of optimistic locking or to request the use of 
pessimistic locks. There are 8 lock modes (see more details here). For now, we will use the 
OPTIMISTIC lock mode, which guarantees that both the transaction that obtains the entity read 
lock and any other that tries to change that entity instance will not both succeed. At least one 
will fail, but like the database isolation levels, which one fails depends upon the implementation. 
The lock modes can be used with different methods. Here, we will use the 
EntitiyManager::lock() method.  
26. Go to method update() of the StudentBean EJB and insert the following line right be-
fore the lines that modify the student attributes: 
// finds the student ... 
entityManager.lock(student, LockModeType.OPTIMISTIC); 
// updates the fields ... 
Your code should be something like this: 
public void update(String username, String password, String name, String email,  
                                                               long courseCode) { 
  Student student = entityManager.find(Student.class, username); 
 
  if (student == null) { 
    System.err.println("ERROR_STUDENT_NOT_FOUND: " + username); 
    return; 
  } 
  entityManager.lock(student, LockModeType.OPTIMISTIC); 
  student.setPassword(password); 
  student.setName(name); 
  student.setEmail(email); 
 
  if (student.getCourse().getCode() != courseCode) { 
    Course course = entityManager.find(Course.class, courseCode); 
    if (course == null) { 
      System.err.println("ERROR_COURSE_NOT_FOUND: " + courseCode); 
      return; 
    } 
    student.setCourse(course); 
  } 
} 
CHECKPOINT 
Deploy your application, check that the logs are good and try to update a student. 
Desenvolvimento de Aplicações Empresariais  Worksheet 5 
Eng.ª Informática- 3.º ano – Ramo SI - 2025-26-1S 
 11 / 11 
For more information on controlling concurrent access and their usage, consult: Locking and 
Concurrency - Jakarta Persistence 
Bibliography 
Java EE Tutorial 8 (all of it). 
 